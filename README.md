# ThomasMore-Examen-Angular-API

# Add appsettings.json file with following contents:

{
  "AppSettings": {
    "Secret": "<put in secret>"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "WebAPiDatabase": "<enter connection string>"
  }
